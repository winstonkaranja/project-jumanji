{
    "dockerfile_lines":[
      "RUN apt-get update && apt-get install -y libgl1-mesa-glx libglib2.0-0 && rm -rf /var/lib/apt/lists/*"
    ],
    "graphs": {
      "advisor": "./recommendation.py:advisor_graph"
    },
    "dependencies": ["."],
    "python_version": "3.11",
    "env": ".env"
  }